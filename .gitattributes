# 统一换行符处理
# gradlew 是 shell 脚本，必须保持 LF，否则在 macOS / Linux 上无法执行
gradlew text eol=lf
gradlew.bat text eol=crlf
*.sh text eol=lf

# 文本文件正常处理
*.java text
*.json text
*.gradle text
*.properties text
*.md text
*.yml text

# 二进制文件不做换行转换
*.jar binary
*.png binary
*.ogg binary
